# 语言引导医学图像分割:文本信息利用实验报告

> 面向导师汇报用 · 2026-08
> 项目:LanGuideMedSeg-MICCAI2023(QaTa-COV19-v2,语言引导 COVID-19 病灶分割)
> 分支:`main`(原始可运行)、`exp/text-ablation`、`exp/aux-supervision`

---

## 1. 实验目的

原始动机是复现论文现象:**单独使用方位句(第 3 句)的分割效果优于完整三句**("更少文本反而更好")。围绕这一点,系统设计了三类实验来回答:

1. **不同信息量的文本(整句/关键词)如何影响分割?**(消融)
2. **能否用文本属性做辅助监督,提升分割?**(多任务)
3. **能否让模型"分割出正确数量的感染区域"?**(计数监督)
4. 深层问题:**模型到底"利用"了文本的哪些信息?**(位置 / 数量 / 性质)

---

## 2. 实验方法

### 2.1 文本特征解析
QaTa 每条描述 = 3 句逗号分隔文本(正则解析,无额外 NLP 依赖):

| 句 | 属性 | 示例 |
|---|---|---|
| 第 1 句 | 性质 nature | `Bilateral pulmonary infection` |
| 第 2 句 | 数量 quantity | `two infected areas` |
| 第 3 句 | 方位 location | `all left lung and middle lower right lung.` |

解析出:nature(0/1)、quantity(1~4)、location 6 维肺区编码(UL/ML/LL/UR/MR/LR)。解析覆盖率 99.75%+。

### 2.2 三条实验路线
1. **前端文本替换消融**(E1-E10):把输入文本换成不同变体(full / 单句 / 结构化关键词),其余不变。
2. **辅助多任务监督**(C4, E11-E22):从解码特征接分类头,用解析属性监督(数量/性质/方位头)。
3. **连通域计数监督**(C1-C3):可微欧拉计数代理 + 精确连通域评估。

### 2.3 数据与设置
- train 7145 / test 2113;图像 224×224;冻结 CXR-BERT(文本)+ ConvNeXt(视觉)+ UNETR 解码器 + GuideDecoder 交叉注意力。
- 统一超参(batch 16, lr 3e-4, patience 20, 按 val_loss 选点)。

---

## 3. 评估方法(重要)

**像素级指标**(test_dice / test_MIoU):走原始 `trainer.test`,一直可靠。
**区域计数指标**(count_acc):对分割图做 8-连通域计数,与真实 GT 连通域数比对;**必须先做面积过滤**(滤 <200px 噪声碎片)。

> ⚠️ **评估 bug 及修正**:模型输出 `out` 已是 sigmoid 概率,早期分析脚本误再套一次 sigmoid(双重 sigmoid),造成假碎片化。修正阈值(`out>0.5`)后全部 count_acc 已重评:**基线从 87.3% 修正为 90.7%**,各方法"提升幅度"整体大幅缩小(详见 §6)。

---

## 4. 实验结果总览

### 4.1 文本消融(单句/关键词 vs 完整句)
| 实验 | 文本输入 | test_dice |
|---|---|---|
| **E1 full** | 完整三句(基线) | **0.8947** |
| E2 location | 仅方位句 | 0.8897 |
| E5 keyword | 结构化关键词 | 0.8881 |
| E10 kw_location | 仅方位关键词 | 0.8842 |
| E4 quantity | 仅数量句 | 0.8308 |
| E3 nature | 仅性质句 | 0.8273 |
| E9 kw_quantity | 仅数量关键词 | 0.8340 |
| E8 kw_nature | 仅性质关键词 | 0.8328 |

### 4.2 辅助/计数监督(基于 full 文本)
| 实验 | 方法 | test_dice | count_acc(过滤200px) |
|---|---|---|---|
| E1 full | 基线 | **0.8947** | 90.7% |
| C4 | 数量分类头(加权CE) | 0.8904 | **91.3%** |
| E16 | FiLM 文本调制 | 0.8913 | 91.0% |
| E21 | 数量头+多层文本 | 0.8888 | 90.8% |
| E15 | clip_clean(去碎片对齐) | 0.8922 | 90.5% |
| E11 | 性质分类头 | 0.8896 | 91.2% |
| E13 | 解冻 BERT | 0.8914 | 90.1% |
| E22 | 侧别门控 | 0.8486 | 87.8% |
| E18 | 数量头+方位句文本 | 0.8613 | **70.8%** |
| C1-C3 | 连通域回归监督 | 0.24~0.36 | —(崩溃) |

---

## 5. 核心结论

1. **"更少文本反而更好"未复现**——完整三句仍最优(0.8947)。
2. **分割主要由"方位"信息驱动**:方位(≈0.89)远强于性质/数量(≈0.83)。
3. **数量词是可靠信号**:与真实连通域一致率 97.2/98.3%(Spearman 0.95+),并非噪声。
4. **连通域"回归"监督不可行**(dice 崩溃);"分类头"监督可行但**修正后增益有限**(+0.6pp)。
5. **没有任何方法能显著超越基线**;但 C4/FiLM/数量头组合**在不损害分割的同时,区域计数略准**。

---

## 6. 发现的问题

1. **评估脚本双重 sigmoid bug**(已修复):造成"假碎片化",把基线 count_acc 从 90.7% 压到 87.3%,并制造了"性质头损害区域计数(81.7%)"等**假象**。
2. **连通域回归损失与像素级分割本质冲突**:模型学会"投机输出两个连通域"来命中多数类(two 占 74%),dice 崩坏;类别加权/降权均无法救回。
3. **多数"文本利用增强"方法无提升**:解冻编码器、多层文本、TANDA、CLIP 对齐、侧别门控等均 ≤ 基线。
4. **数量类严重不平衡**(two 74%):任何基于数量的监督都需处理不平衡。

---

## 7. 关注点详细分析

### 7.1 碎片干扰是否还影响模型分割效果?

**结论:修正评估后,碎片不再显著干扰分割效果;此前的"碎片化"主要是评估 bug 假象。**

| 评估方式 | 基线 count_acc | 说明 |
|---|---|---|
| 错误(双重 sigmoid) | ≈0%(未过滤)/ 87.3%(过滤200) | 假碎片使连通域暴涨到 8~16 |
| **修正(out>0.5)** | **90.7%(过滤200)** | 分割图连通域本身干净 |

修正后的实证(基线某 batch 8 张图连通域):`out>0.5` → `[2,2,2,2,2,2,2,2]`(=真实区域数);错误 `sigmoid(out)>0.5` → `[6,5,6,10,...]`(假碎片)。

**解读**:用正确阈值,模型分割图的连通域数量已与真实区域数一致,碎片不再是主要问题;面积过滤仍是评估规范(去除真噪声),但不是"救活"指标的关键。

### 7.2 模型是否只学习到了位置信息?

**结论:位置是"主导"信息,但模型并非"只"学位置——完整文本仍有额外增益。**

| 证据 | 数据 |
|---|---|
| 位置信息强 | E2 location 0.8897、E10 kw_location 0.8842 ≈ 完整句 0.8947 |
| 性质/数量弱 | E3/E4/E8/E9 ≈ 0.83(明显低) |
| **但非"只"学位置** | **E1 full 0.8947 > E2 location 0.8897**(完整文本仍略优) |

**解读**:交叉注意力让模型主要"读"位置信息(因为位置直接决定"在哪分割");但完整三句仍提供少量额外收益(数量/性质与位置的冗余耦合提供了微弱约束),说明模型并非完全忽略其他信息。

### 7.3 为什么方位短语比整句下降,而性质/数量反而提升?

**结论:方位句"信息量大",精简损失大;性质/数量句"信息简单且冗余",精简去除噪声有微增益。**

| 属性 | 整句 | 关键词 | 差异 | 平均 token |
|---|---|---|---|---|
| 方位 | E2 0.8897 | E10 0.8842 | **-0.0055(降)** | 9.38 |
| 性质 | E3 0.8273 | E8 0.8328 | **+0.0055(升)** | 5.00 |
| 数量 | E4 0.8308 | E9 0.8340 | **+0.0032(升)** | 8.02 |

**解释**:
- **方位句**常含"多个区域用 and 连接"的完整组合(如 `all left lung and middle lower right lung`),精简成关键词会**丢失区域组合细节** → 下降。
- **性质/数量句**信息简单且与位置信息**冗余**(nature×quantity Cramer's V=0.979),句子里的句法冗余/噪声对分割是负担;精简关键词**去掉冗余 → 少量提升**。
- 数据支撑:方位句平均 9.38 token(信息密度高),性质句仅 5 token、数量句 8 token(信息简单)。

### 7.4 单句实验现象对比数据

| 对比组 | 整句 | 关键词 | 完整句 | 现象 |
|---|---|---|---|---|
| 方位 | E2 0.8897 | E10 0.8842 | E1 0.8947 | 关键词<整句<完整 |
| 性质 | E3 0.8273 | E8 0.8328 | E1 0.8947 | 关键词>整句,均远低于完整 |
| 数量 | E4 0.8308 | E9 0.8340 | E1 0.8947 | 关键词>整句,均远低于完整 |

- **所有单句/关键词都低于完整三句** → "更少文本更好"未复现。
- 单句内部:**方位最优**(0.88+),性质/数量最弱(≈0.83)。
- 关键词 vs 整句:**方位精简受损,性质/数量精简微益**(见 §7.3)。

### 7.5 目前提升比较有效的实验结果

修正评估后,**没有任何方法能显著超越基线**,但以下方法在"保持分割质量 + 区域计数略准"上相对有效:

| 排名 | 方法 | test_dice | count_acc(200px) | 相对基线 |
|---|---|---|---|---|
| 🥇 | C4 数量分类头 | 0.8904 | **91.3%** | +0.6pp |
| 🥈 | FiLM 文本调制 | 0.8913 | 91.0% | +0.3pp |
| 🥈 | 数量头+多层文本(E21) | 0.8888 | 90.8% | +0.1pp |
| — | **基线 E1** | **0.8947** | 90.7% | — |

**有效共性**:都围绕"**数量语义的显式/结构引导**"且**不干预像素目标函数**(分类头/FiLM 调制),因此不损害 dice 又能微调区域结构。
**无效方向**:连通域回归监督(崩溃)、全局对齐(略降)、侧别门控(明显降)、数据增强/解冻/多层文本(持平)。

---

## 8. 总结

- **核心发现**:本数据集上"更少文本更好"未复现;位置信息主导分割;数量词可靠但类别不平衡;连通域"回归"监督不可行、"分类头"监督增益有限。
- **有效方向**:数量分类头(C4)、FiLM 调制是仅有的、能"不损分割且区域计数略准"的方法。
- **方法论教训**:评估脚本需严格验证模型输出类型(概率 vs logits),否则会系统性地扭曲结论。
